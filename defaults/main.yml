---
# defaults file for ansible_role_epel_release

epel_release_installed: true # Default: true. Install epel-release if absent

epel_release_state: "{{ 'present' if epel_release_installed==true else 'absent' }}"
