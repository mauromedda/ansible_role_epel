---
# defaults file for ansible_role_epel-release

epel-release_installed: true # Default: true. Install epel-release if absent

epel-release_state: "{{ 'present' if epel-release_installed==true else 'absent' }}"
